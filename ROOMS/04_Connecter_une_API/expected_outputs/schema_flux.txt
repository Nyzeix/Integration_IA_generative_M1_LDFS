Schema du flux - Room 04
=========================
Le serveur ne s'execute pas. Sans message erreur fourni.

Completez ce schema en decrivant ce qui circule a chaque etape.

Client Python (12_client_local.py)
    |
    |  [1] Requête HTTP POST vers http://127.0.0.1:8000/question
    |      Données envoyées : [JSON, avec APIKey, message, à minima.]
    |      Clé API présente ? [X] oui  [ ] non
    v
Serveur FastAPI (mini_api_fastapi.py)
    |
    |  [2] Appel API vers OpenAI
    |      Données envoyées : [JSON avec APIKey, message, à minima.]
    |      Clé API présente ? [X] oui  [ ] non
    v
Serveur Groq (distant)
    |
    |  [3] Le modèle GPT génère la réponse
    |      Données retournées : [Message de réponse]
    v
Serveur FastAPI
    |
    |  [4] Construction de la réponse HTTP
    |      Données retournées au client : [JSON, message de réponse]
    v
Client Python
    |
    |  [5] Affichage de la reponse dans le terminal


Questions à remplir :
- Où se trouve la clé API ? (côté client ou côté serveur ?)
  Réponse : [Dans le message du début avec la question]

- Pourquoi est-ce une bonne pratique de garder la cle cote serveur ?
  Reponse : [votre reponse]
